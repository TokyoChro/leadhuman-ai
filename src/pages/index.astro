---
import BaseLayout from "../layouts/BaseLayout.astro";
import PostCard from "../components/PostCard.astro";
import { getCollection } from "astro:content";

const buildPosts = (await getCollection("build", ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 2);

const leadPosts = (await getCollection("lead", ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 2);

function getReadingTime(body: string | undefined) {
  if (!body) return 0;
  return Math.ceil(body.split(/\s+/).length / 200);
}
---

<BaseLayout title="Home">
  <!-- Hero -->
  <section class="py-16 md:py-24 hero-text">
    <div class="flex flex-col md:flex-row md:items-center gap-10">
      <div class="flex-1">
        <h1 class="text-4xl sm:text-5xl font-bold tracking-tight leading-tight text-heading">
          Lead humanly.<br />Learn constantly.<br />Build with AI.
        </h1>
        <p class="mt-6 text-lg text-muted max-w-xl">
          Thoughts on AI, leadership, learning, and building things that matter.
        </p>
        <p class="text-sm text-stone-500 mt-2">
          Jay Vergara &middot; Leadership, AI, and cross-cultural insights from Tokyo
        </p>
      </div>
      <div class="flex-1">
        <div class="relative w-full rounded-2xl overflow-hidden shadow-md" style="aspect-ratio: 16/9;">
          <iframe
            src="https://www.youtube.com/embed/7q2wFn8qZEU"
            title="Welcome to leadhuman.ai"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            class="absolute inset-0 w-full h-full"
          ></iframe>
        </div>
      </div>
    </div>
  </section>

  <!-- Latest from Lead Humanly -->
  <section class="mb-16">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-ivy">Latest from Lead Humanly</h2>
      <a href="/lead/" class="text-sm text-ivy hover:text-ivy-light transition-colors">View all &rarr;</a>
    </div>
    <div class="grid gap-4 sm:grid-cols-2">
      {leadPosts.map((post) => (
        <PostCard
          title={post.data.title}
          description={post.data.description}
          pubDate={post.data.pubDate}
          href={`/lead/${post.id}/`}
          category="lead"
          readingTime={getReadingTime(post.body)}
          image={post.data.image}
        />
      ))}
    </div>
  </section>

  <!-- Latest from Build with AI -->
  <section class="mb-16">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-ivy">Latest from Build with AI</h2>
      <a href="/build/" class="text-sm text-ivy hover:text-ivy-light transition-colors">View all &rarr;</a>
    </div>
    <div class="grid gap-4 sm:grid-cols-2">
      {buildPosts.map((post) => (
        <PostCard
          title={post.data.title}
          description={post.data.description}
          pubDate={post.data.pubDate}
          href={`/build/${post.id}/`}
          category="build"
          readingTime={getReadingTime(post.body)}
          image={post.data.image}
        />
      ))}
    </div>
  </section>

  <!-- CTA -->
  <section class="text-center py-12">
    <a
      href="https://linkedin.com/in/vergarajay/"
      target="_blank"
      rel="noopener noreferrer"
      class="inline-block px-6 py-3 bg-[#0A66C2] text-white font-semibold rounded-lg hover:bg-[#004182] transition-colors"
    >
      Connect with me on LinkedIn
    </a>
  </section>
</BaseLayout>
