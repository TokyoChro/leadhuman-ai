---
import BaseLayout from "../../layouts/BaseLayout.astro";
import PostCard from "../../components/PostCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("lead", ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

function getReadingTime(body: string | undefined) {
  if (!body) return 0;
  return Math.ceil(body.split(/\s+/).length / 200);
}
---

<BaseLayout title="Lead Humanly" description="Posts about leadership, learning, and cross-cultural communication.">
  <h1 class="text-3xl font-bold mb-2 text-heading">Lead Humanly</h1>
  <p class="text-muted mb-8">Leadership, learning, and cross-cultural communication.</p>

  <div class="grid gap-4 sm:grid-cols-2">
    {posts.map((post) => (
      <PostCard
        title={post.data.title}
        description={post.data.description}
        pubDate={post.data.pubDate}
        href={`/lead/${post.id}/`}
        category="lead"
        readingTime={getReadingTime(post.body)}
      />
    ))}
  </div>

  {posts.length === 0 && (
    <p class="text-muted italic">No posts yet. Check back soon.</p>
  )}
</BaseLayout>
